export type CustomInputFieldProps = {
  type: string;
  backgroundColor: string;
  border: string;
  borderRadius: number;
  fontSize: string;
  padding: number;
  width: string;
  placeholder: string;
  onChange: (value: string) => void;
};
